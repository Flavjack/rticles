
`r if (knitr:::is_html_output()) '# References'`
`r if (!knitr:::is_html_output()) '# References'`

```{r files}
if(!dir.exists("img")) {dir.create("img")}
if(!dir.exists("img/files")) {dir.create("img/files")}
if(exists("fb")){openxlsx::write.xlsx(fb,"img/files/data.xlsx")}
if(dir.exists("docs"))
  {zip::zipr(zipfile = "docs/files.zip", files = "img/files")}
knitr::write_bib(c(.packages()),'style/pkgs.bib')
```

```{r debug, eval=FALSE, include=FALSE}
if(file.exists("rticles.Rmd")){file.remove("rticles.Rmd")}
if(dir.exists("libs")){fs::dir_delete("libs")}
if(dir.exists("docs")){fs::dir_delete("docs")}
```

